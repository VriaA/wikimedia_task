import type { Option } from '../types/editor';
const dir: Option[] = [
  { id: 'ltr', value: 'ltr', label: 'Left to right' },
  { id: 'rtl', value: 'rtl', label: 'Right to left' }
];

export default dir;
